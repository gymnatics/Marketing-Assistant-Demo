apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: imagegen-mcp
  namespace: marketing-assistant-v2
spec:
  to:
    kind: Service
    name: imagegen-mcp
  port:
    targetPort: 8091
  tls:
    termination: edge
