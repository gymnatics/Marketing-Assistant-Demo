apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    app.openshift.io/connects-to: '["creative-producer","customer-analyst","delivery-manager"]'
  labels:
    app: campaign-director
    app.kubernetes.io/component: backend
    app.kubernetes.io/instance: campaign-director
    app.kubernetes.io/part-of: simon-casino-agents
    app.openshift.io/runtime: python
    component: agent
    kagenti.io/framework: LangGraph
    kagenti.io/type: agent
    protocol.kagenti.io/a2a: ''
  name: campaign-director
spec:
  strategy:
    type: Recreate
  replicas: 1
  selector:
    matchLabels:
      app: campaign-director
  template:
    metadata:
      labels:
        app: campaign-director
        app.kubernetes.io/component: backend
        app.kubernetes.io/instance: campaign-director
        app.kubernetes.io/part-of: simon-casino-agents
        app.openshift.io/runtime: python
        component: agent
    spec:
      containers:
      - envFrom:
        - configMapRef:
            name: marketing-assistant-config
        - secretRef:
            name: marketing-assistant-secrets
        image: quay.io/rh-ee-dayeo/marketing-assistant:campaign-director
        imagePullPolicy: Always
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 30
        name: agent
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 100m
            memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: campaign-director
spec:
  ports:
  - name: a2a
    port: 8080
    targetPort: 8080
  selector:
    app: campaign-director
