apiVersion: v1
kind: Secret
metadata:
  name: marketing-assistant-secrets
type: Opaque
stringData:
  # Model endpoints and tokens — change these for your cluster
  CODE_MODEL_ENDPOINT: "https://<your-code-model-route>/v1"
  LANG_MODEL_ENDPOINT: "https://<your-lang-model-route>/v1"
  IMAGEGEN_MODEL_ENDPOINT: "https://<your-imagegen-model-route>/v1"
  MONGODB_URI: "mongodb://mongodb:27017"
