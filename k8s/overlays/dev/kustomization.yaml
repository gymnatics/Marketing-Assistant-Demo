apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  # secret.yaml is applied separately (contains real tokens, not included in oc apply -k)

namespace: 0-marketing-assistant-demo

patches:
  - path: configmap-patch.yaml
