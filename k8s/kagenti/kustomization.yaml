apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Namespace is set by the caller, e.g. oc apply -k k8s/openshift -n marketing-assistant-v2

resources:
  - envoy-config.yaml
  - spiffe-helper-config.yaml
  - authbridge-config.yaml
  - authproxy-routes.yaml